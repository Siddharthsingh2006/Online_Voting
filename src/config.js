// 🌐 Backend URL
// When running locally:
const API_BASE_URL = "http://localhost:8081/api";

// When you deploy backend (for example on Render or Railway), replace it with:
// const API_BASE_URL = "https://your-backend-deployment-url/api";

export default API_BASE_URL;
